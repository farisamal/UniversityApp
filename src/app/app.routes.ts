import { Routes } from '@angular/router';
import { StudetListComponent } from './studet-list/studet-list.component';

export const routes: Routes = [
  { path: 'student-list', component: StudetListComponent }
];
